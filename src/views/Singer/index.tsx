import React, { useState } from "react";
import PropTypes from "prop-types";
import _Album from "../../types/_Album";

function SingerPage() {
  const [album, setAlbum] = useState({} as _Album);

  

  return (
    <div className="detail-page">
      text
    </div>
  );
}

export default SingerPage;
