import React from 'react';
import PropTypes from 'prop-types';

SongList.propTypes = {
    
};

function SongList( ) {
    return (
        <div>
            hello
        </div>
    );
}

export default SongList;